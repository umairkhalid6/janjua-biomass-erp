{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "node -e \"console.log('DATABASE_URL present:', !!process.env.DATABASE_URL, '| length:', (process.env.DATABASE_URL||'').length)\" && npx prisma migrate deploy",
    "startCommand": "npm run start",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
